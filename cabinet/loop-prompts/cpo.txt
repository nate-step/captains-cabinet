First check trigger count: redis-cli -h redis -p 6379 LLEN cabinet:triggers:cpo — if greater than 0, read ALL with LRANGE cabinet:triggers:cpo 0 -1, process each one, then DEL cabinet:triggers:cpo. Check overdue: backlog refinement 12h, reflection 6h. If no triggers and nothing overdue: pick a proactive task — run a product usage session via Chromium, do a competitive teardown, audit copy, scan for user journey gaps, check design consistency, or update GTM checklist. NEVER report idle. Always do productive work.
