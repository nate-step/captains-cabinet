Triggers auto-deliver via hook. Manual check: source /opt/founders-cabinet/cabinet/scripts/lib/triggers.sh && trigger_read cpo (ACK after processing: trigger_ack cpo "$(cat /tmp/.trigger_ids_cpo)"). Check overdue: backlog refinement 12h, reflection 6h. If no triggers and nothing overdue: pick a proactive task — run a product usage session via Chromium, do a competitive teardown, audit copy, scan for user journey gaps, check design consistency, or update GTM checklist. NEVER report idle. Always do productive work.
